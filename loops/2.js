const pessoa = [
    'jonas',
    'Gomez',
    2037 - 1998,
    'medico',
    ['michale', 'joao', 'carlos']
];

// inverter impressão 
for (let i = pessoa.length - 1; i >= 0; i--) {
    console.log(i, pessoa[i]);
}

